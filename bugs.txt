Bugs :

PROFIL
- photo de profil par défaut
- modification de la photo

CREER/EDITER UNE LISTE
- quand on sélectionne un ami fb, L'utilisateur est directement redirigé vers ses listes 
- concurrence : Si un utilisateur A supprime une liste et qu'un autre utilisateur B la modifie en même temps, la liste existera toujours pour B et plus pour A (comme si A s'était désinscrit de la liste)

ITEMS
- modifier un item : le champ devrai se transformer en champ éditable quand on veut éditer l'item (ici : redirection)
- pas de gestion de la concurrence (si un utilisateur modifie un item et qu'un autre utilisateur le supprime en même temps, la base de données affiche une erreur)

NOTIFICATIONS
- Si un utilisateur consulte ses notifications et fait retour, le compteur de notifications n'est pas remis à 0
